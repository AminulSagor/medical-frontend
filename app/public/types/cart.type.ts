export type CartItem = {
  id: string;
  name: string;
  ref: string;
  price: number;
  qty: number;
  imageUrl: string;
};

export type UpsellItem = {
  id: string;
  name: string;
  price: number;
  imageUrl: string;
};